
    <!-- Hero Banner (editable section — manage in Shopify Theme Customizer) -->
    {% section 'hero-banner' %}

    <!-- Category Grid (editable section — manage in Shopify Theme Customizer) -->
    {% section 'featured-categories' %}

    <!-- Campaign Banners (editable section — manage in Shopify Theme Customizer) -->
    {% section 'campaign-banners' %}

    <!-- Editorial Split (editable section — manage in Shopify Theme Customizer) -->
    {% section 'editorial-split' %}
    
    <!-- New Arrivals (editable section — pick products in Shopify Theme Customizer) -->
    {% section 'new-arrivals' %}
